FROM node:20-alpine
WORKDIR /app
RUN apk add --no-cache curl openssl openssl-dev

COPY package.json tsconfig.json ./
RUN npm install
COPY prisma ./prisma
RUN npx prisma generate

EXPOSE 3010
CMD ["npm", "run", "dev"]
